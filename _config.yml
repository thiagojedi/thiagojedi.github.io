timezone: America/Recife
encoding: UTF-8

defaults:
  -
    scope:
      path: "" # empty string for all files
    values:
      layout: "default"
  -
    scope:
      path: "_posts/"
    values:
      layout: "post" # overrides previous default layout